{
  "name": "appgeniex-ml-service",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:py": "python -m pip install -r requirements.txt",
    "dev": "python -m pip install -r requirements.txt && python -m uvicorn app:app --reload --port 5001",
    "lint": "ruff check . && black --check .",
    "format": "black . && ruff check --fix .",
    "test": "pytest -q"
  }
}
